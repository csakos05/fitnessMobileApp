import 'package:flutter/material.dart';

class UpdateMandatoryPage extends StatelessWidget {
  const UpdateMandatoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Update Mandatory Page')),
    );
  }
}
